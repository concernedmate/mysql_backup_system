{
  "name": "mysql_auto_backup",
  "version": "0.0.1",
  "description": "auto backup to mysql dbs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "afif azizy",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.10.2"
  }
}
